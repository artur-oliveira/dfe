package org.dfe.interfaces.internal.nf;

/**
 * This code is defining a Java interface named `NfceURL` that extends another interface named `NfCommonServiceURL`. By
 * extending `NfCommonServiceURL`, `NfceURL` inherits all the methods and constants defined in `NfCommonServiceURL`. This
 * allows `NfceURL` to add additional methods or constants specific to its own functionality while still maintaining the
 * methods and constants defined in `NfCommonServiceURL`.
 */
public interface NfceURL extends NfCommonServiceURL {


}
